<%= render 'shared/content_title', title: "Edit Profile: #{current_user.username}" %>
<%= render 'shared/form_errors', form_obj: current_user %>
<%= render partial: 'form', locals: { user: current_user, url_path: edit_profile_path, method: 'patch' } %>